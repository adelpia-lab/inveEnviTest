import Button from '@mui/material/Button';

export default function TextButton( {Ids, TextLabel, OnClickFunction}){
    <div>
        <Button onClick={OnClickFunction.onClick}> This is Magic. </Button>
    </div>
}